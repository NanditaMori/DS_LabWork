/*
    A 75.Write a menu driven program to implement Binary Search Tree (BST) & perform following operations:
•Insert a node
•Delete a node
•Search a node
•Preorder Traversal
•Postorder Traversal
•Inorder Traversal

*/

struct node{
    int info;
    struct node *lptr;
    struct node *rptr;
};

// struct node *tree = (struct node *)malloc(sizeof(struct node));

void insertNode(int x)
{
    
}

int main()
{
    while(1)
    {
        int choice;

        printf("Enter your choice:");
        scanf("%d",&choice);

        switch(choice)
        {
            case 1:
            {
                int x;
                printf("Enter a value to be inserted: \n");
                scanf("%d",&x);

            }
        }
    }
}